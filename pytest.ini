[pytest]
addopts = -q
# Skip the vendored reference repos (alphazero/refs) and virtualenvs, which
# contain their own unrelated test files that break collection.
norecursedirs = .venv */.venv refs node_modules dist build __pycache__
